language: python
cache: pip
python: "2.7"
install:
  - pip install ansible
script:
  - ansible-playbook --syntax-check -i production site.yml
  - ansible-playbook -i production site.yml
branches:
  only:
    - master