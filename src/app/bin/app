#!/bin/bash

cd "$(dirname $BASH_SOURCE[0])/.."

if [ "$NODE_ENV" == "dev" ]; then
   mkdir -p /tmp/app
   NODE_PATH=node_modules:lib NODE_PORT=3000 HOME=/tmp/app node_modules/nodemon/bin/nodemon.js --exitcrash app.js
else
  NODE_PATH=node_modules:lib NODE_PORT=3000 node app.js
fi